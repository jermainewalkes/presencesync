# Shared
msal>=1.31.0
msal-extensions>=1.2.0
requests>=2.32.0
keyring>=25.0.0

# macOS front end
rumps>=0.4.0 ; sys_platform == "darwin"

# Windows front end
pystray>=0.19 ; sys_platform == "win32"
pillow>=10.0 ; sys_platform == "win32"

# Packaging (install when building): py2app (macOS)
